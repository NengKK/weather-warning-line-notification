test('get-weather-warning-news', () => {
  test('should get weather warning news from tmd.go.th and send forward to SQS successful', () => {
    expect(false).toBeTruthy();
  });

  test('should handle erro incase of tmd.go.th is not available', () => {
    expect(false).toBeTruthy();
  });
});
